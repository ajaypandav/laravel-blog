<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateBlogCategoryTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('blog_category', function (Blueprint $table) {
            $table->id();
            $table->bigInteger('bid')->unsigned();
            $table->bigInteger('cid')->unsigned();
            $table->foreign('bid')->references('id')->on('blog')->onDelete('cascade')->onUpdate('no action');
            $table->foreign('cid')->references('id')->on('category')->onDelete('restrict')->onUpdate('no action');
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('blog_category');
    }
}
